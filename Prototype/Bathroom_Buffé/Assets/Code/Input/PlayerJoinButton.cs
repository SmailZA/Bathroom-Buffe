using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerJoinButton : MonoBehaviour
{
    public InputType[] inputType;

    // Have all types of input join as this player index
    // list remaining indices & remaining available inputs to join with
    // (stop 'space' from making a player join as this index if one has joined with 'space')

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
